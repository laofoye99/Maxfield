---
title: "Hello World"
titleZh: "你好，世界"
date: "2026-04-29"
summary: "First post — testing Markdown, math formulas, and code highlighting."
tags: ["math", "python", "notes"]
---

## Markdown

**Bold text**, *italic text*, `inline code`, and [a link](https://github.com/laofoye99).

> A blockquote with a green left border.

---

## Math — KaTeX

Inline: $E = mc^2$

Block:

$$
\int_{-\infty}^{\infty} e^{-x^2}\, dx = \sqrt{\pi}
$$

$$
A = \begin{pmatrix} a & b \\ c & d \end{pmatrix}, \qquad \det(A) = ad - bc
$$

---

## Code

```python
import numpy as np

def gaussian(x: np.ndarray, mu: float = 0, sigma: float = 1) -> np.ndarray:
    return np.exp(-0.5 * ((x - mu) / sigma) ** 2) / (sigma * np.sqrt(2 * np.pi))
```

```typescript
const greet = (name: string): string => `Hello, ${name}!`;
console.log(greet("Yuxin"));
```

---

## Custom components

<Callout type="tip">
  Use Callout, Steps, Step, and Details components directly in any MDX file.
</Callout>

<Details summary="Show more detail">
  This content is hidden until the reader expands it.
</Details>

<Steps>
  <Step n={1} title="Write a markdown file">
    Create a new .mdx file inside content/blog/.
  </Step>
  <Step n={2} title="Add frontmatter">
    Set title, date, summary, and tags at the top of the file.
  </Step>
  <Step n={3} title="Push to GitHub">
    git push triggers GitHub Actions and deploys automatically.
  </Step>
</Steps>